//here we should write logic that will check incoming date with current date, if task time is older than now - return true
const isTaskExpired = (taskDate: Date): boolean => {
  const currentDate = new Date();
  return false;
};

export default isTaskExpired;
